# VoiceMind（FunASR）项目结构

基于阿里达摩院 SenseVoiceSmall / paraformer-zh-streaming 的本地语音识别系统，
提供**文件上传识别、说话人分离、TXT/SRT 导出、实时录音流式字幕、批量字幕生成**五项能力。

## 文件夹说明

| 文件夹 | 用途 |
|--------|------|
| `code/` | 所有 Python 脚本（服务端、批量工具、测试） |
| `audio/` | 音频文件（wav / mp3 / m4a / flac 等） |
| `video/` | 视频文件（mp4 / mov / mkv 等） |
| `subtitle/` | 生成的字幕文件（srt / txt），批量脚本的默认输出目录 |
| `web/` | 前端页面（index.html、audio-processor.js） |
| `docs/` | 说明文档 |
| `funasr_env/` | Python 虚拟环境（**不要移动或删除**） |
| `.workbuddy/` | 开发记录与备份（**不要删除**） |

## 目录树

```
FunASR/
├── 启动VoiceMind.bat           ← ★ 双击这里启动全部服务并自动打开浏览器
├── 停止VoiceMind.bat           ← ★ 双击停止全部服务
├── FunASR.ico               # 启动器图标（桌面快捷方式引用）
├── code/                    ← 所有代码
│   ├── launcher.py          # 一键启动器：拉起两个服务、等端口就绪、开浏览器
│   ├── stop.py              # 停止脚本：按 PID + 端口结束服务
│   ├── app.py               # FastAPI 服务：:8000，页面托管 + 上传识别 + 说话人分离 + 导出
│   ├── ws_server.py         # 独立 WebSocket 流式识别服务：:8001
│   ├── batch_subtitle.py    # 批量给音视频生成 SRT 字幕
│   ├── test_ws.py           # 流式识别自检客户端（无需麦克风，缺样本会自动用 ffmpeg 转）
│   ├── test.py              # 早期的最小识别验证脚本
│   └── main.py              # PyCharm 新建项目自带的示例文件（模板，可忽略）
├── audio/                   ← 所有音频
├── video/                   ← 所有视频
├── subtitle/                ← 所有字幕
├── web/                     ← 前端
│   ├── index.html           # 专业深色界面（Linear 风格）：顶栏 + 侧边导航 + 底部状态栏，
│   │                        #   四个功能页：普通识别 / 说话人分离 / 关键词检索 / 实时录音
│   └── audio-processor.js   # AudioWorklet 采集器（每 600ms 一块）
├── logs/                    ← 启动器生成的运行日志（app.log / ws.log / pids.json）
├── docs/                    ← 说明文档
├── 项目结构说明.md
├── funasr_env/              ← 虚拟环境（勿动）
└── .workbuddy/              ← 开发记录与整理前备份（勿删）
```

## 快速开始（桌面方式，推荐）

**双击 `启动VoiceMind.bat`（或桌面快捷方式「VoiceMind 声智助手」）即可**：
不弹任何终端窗口，服务在后台静默启动，端口就绪后自动打开浏览器。
服务运行中再次双击：只重新打开页面，不会重复启动。

- **启动只需 2~3 秒**：模型改为按需加载，端口先就绪、浏览器立即打开
- **模型在首次使用时才加载（约 30~90 秒）**：页面打开时会自动在后台预热，
  页面上有状态条显示"模型已就绪 / 正在加载"；加载期间直接提交识别即可，会自动等待
- **停止方式**：双击 `停止VoiceMind.bat`（无窗口模式下没有"关窗口即停止"，必须用这个）
- **排查问题看日志**：`logs\launcher.log`（启动器）、`logs\app.log`、`logs\ws.log`（两个服务）

## 快速开始（命令行方式）

> 所有命令都在**项目根目录**执行。虚拟环境在根目录，脚本在 `code/`，
> 所以用 `funasr_env\Scripts\python` 直接指定解释器，无需手动 activate。

### 1. 启动 Web 服务（页面 + 上传识别 + 说话人分离 + 导出）

```powershell
cd C:\Users\iiiis\Desktop\FunASR
funasr_env\Scripts\python code\app.py
```

### 2. 启动 WebSocket 流式识别服务（实时录音字幕需要）

```powershell
cd C:\Users\iiiis\Desktop\FunASR
funasr_env\Scripts\python code\ws_server.py
```

### 3. 批量生成字幕

```powershell
cd C:\Users\iiiis\Desktop\FunASR
funasr_env\Scripts\python code\batch_subtitle.py
```

默认扫描 `audio/` 和 `video/`，字幕统一输出到 `subtitle/`。常用参数：

| 参数 | 说明 |
|------|------|
| `--force` | 覆盖已存在的字幕（默认跳过，避免白跑模型） |
| `--list` | 只列出待处理文件，不加载模型 |
| `-r` | 递归子目录 |
| `--side-by-side` | 字幕与源文件放同一目录（默认集中到 `subtitle/`） |
| `--outdir D:/subs` | 指定字幕输出目录 |
| `--language auto` | 识别语言，默认 `auto` 自动判定 |

### 4. 流式识别链路自检（不需要麦克风）

```powershell
cd C:\Users\iiiis\Desktop\FunASR
funasr_env\Scripts\python code\test_ws.py
```

不带参数时会自动用 ffmpeg 把 `audio\test.wav` 转成 16k 单声道样本（放到系统临时目录），
也可以手动指定音频：`funasr_env\Scripts\python code\test_ws.py <16k单声道wav路径>`

## 访问地址

| 地址 | 说明 |
|------|------|
| `http://127.0.0.1:8000/index.html` | Web 界面（**推荐用这个地址**） |
| `http://127.0.0.1:8000/` | 同上（根路径） |
| `http://127.0.0.1:8000/docs` | 自动生成的 API 文档 |
| `ws://127.0.0.1:8001` | 流式识别 WebSocket |

## 接口一览

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` `/index.html` | 返回前端页面 |
| GET | `/health` | 健康检查：返回已加载/正在加载的模型（前端状态条用它） |
| POST | `/warmup` | 后台预热基础模型（页面打开时调用，不阻塞） |
| GET | `/audio-processor.js` | AudioWorklet 处理器文件 |
| POST | `/asr` | 上传音频**或视频** → 识别文本；视频额外返回 `segments`（毫秒时间戳分段） |
| POST | `/asr_spk` | 上传音频 → 按说话人分段 |
| POST | `/asr_stream` | 上传短音频片段 → 识别文本（HTTP 轮询，降级备用） |
| POST | `/export_txt` | 上传音频 → 下载带说话人标签的 TXT |
| POST | `/export_srt` | 上传音频 → 下载 SRT 字幕 |
| POST | `/search_keywords` | 上传音频/视频 + 关键词（逗号分隔）→ 命中片段与时间点（毫秒） |
| POST | `/batch_search` | 批量检索：扫描文件夹（留空 = 项目 audio/ 与 video/）下所有音视频 |
| WS | `/ws/asr`（:8000） | 内置流式识别（前端未使用，保留） |
| WS | `/`（:8001） | 独立流式识别服务（前端实时录音走这里） |

## 注意事项

1. **麦克风权限**：浏览器只在 `http://127.0.0.1` / `localhost` 或 https 下允许录音，
   **不要双击 html 文件打开**（`file://` 会被拒绝）。请固定使用同一个地址，
   在 `127.0.0.1` 和 `localhost` 之间切换会重新询问权限。
2. **两个服务要同时开着**：`:8000` 提供页面与文件识别，`:8001` 提供实时流式识别。
3. **首次加载慢**：流式模型 `paraformer-zh-streaming` 约 881MB，
   第一次连接 WebSocket 时会下载（约 1 分钟），之后秒连。
4. **局域网访问**：手机等设备可用 `http://<本机IP>:8000` 打开页面，
   但**麦克风仍会被浏览器拒绝**（非 https 非 localhost），这是浏览器安全策略。
5. **ffmpeg 必需**：非 wav 音频与视频都要靠它转码。
   若 `ffmpeg` 不在 PATH，脚本会回退查找 `C:\ffmpeg-9.0.1-full_build\bin\ffmpeg.exe`。
6. **pip 镜像**：本机代理会拦截默认源与清华源，装包请用阿里云镜像：
   `funasr_env\Scripts\python -m pip install <包名> -i https://mirrors.aliyun.com/pypi/simple/`

## 整理记录

- 整理日期：2026-09-15
- 分类前所有脚本、音频、视频、字幕都在项目根目录，现已按类型归入各文件夹。
- 脚本路径已改为基于 `__file__` 定位（不再依赖当前工作目录），
  因此**从任何目录启动都能正确定位前端页面与素材**。
- 上传临时文件改放系统临时目录，不再在项目里产生 `temp_*.wav`。
- 整理前的源文件备份在 `.workbuddy/backup/2026-09-15_reorg/`，如需回退可从那里取。

## 桌面化改造记录（2026-09-15）

- 新增 `code/launcher.py` 一键启动器：无窗口后台拉起两个服务，端口就绪后自动开浏览器；
  通过 Windows 作业对象保证**关闭启动窗口时后台服务一并退出**，不留僵尸进程。
- 新增 `code/stop.py` + `停止VoiceMind.bat`：先按 `logs/pids.json` 结束启动器，再按端口兜底清理。
- 新增 `启动VoiceMind.bat`（UTF-8 + chcp 65001）与桌面快捷方式「VoiceMind 声智助手」（图标 `FunASR.ico`）。
- 服务输出重定向到 `logs/app.log`、`logs/ws.log`，启动失败时看这里排查。
- `app.py` / `ws_server.py` **无需改动**：两者早已用 `__file__` 推导绝对路径，
  改成 `../web/...` 相对路径反而会依赖启动时的工作目录。

## 关键词检索功能（2026-09-15）

- 前端第 4 个模式「🔍 关键词检索」：输入关键词 + 选择音频/视频 → 展示命中片段
  （时间点 HH:MM:SS + 关键词高亮 + 精确/模糊标记），可导出 TXT；支持批量检索整个文件夹。
- 后端 `/search_keywords`：`sentence_timestamp=True` 取句子级时间戳（**毫秒**，VAD 粒度；
  任务书中 `output_timestamp=True` 是不存在的参数）；视频先经 ffmpeg `-vn` 提取音频。
- 后端 `/batch_search`：`folder` 留空默认扫项目 `audio/` 与 `video/`；按文件分组返回，失败文件单独列出。
- 模糊匹配用 rapidfuzz（阿里云镜像安装）`partial_ratio`，**默认阈值 75 而非 80**：
  实测 4 字词错 1 个字只得 75 分（"用户体验"vs"用户体检"），阈值 80 会漏掉这类错别字；
  3 字词错 1 字只有 66.7 分。前端滑块可调 50~100。
- 精确匹配优先（大小写不敏感），同段可命中多个关键词；rapidfuzz 相似度随结果返回。

## 普通识别支持视频（2026-09-15）

- `/asr` 现在接受音频与视频：视频经 ffmpeg（`-vn` 丢视频流）提取 16k 单声道音频后识别；
  所有上传统一走 ffmpeg 转码，顺带解决 m4a 等格式直接喂模型解码不稳的问题。
- 视频响应含 `type: "video"` 与 `segments`（`sentence_info` 毫秒时间戳分段）；音频响应结构与之前一致。
- 注意：`segments` 的分段文本是标点模型处理**之前**的原始句文本（时间戳对齐回退 VAD 所致），
  `text` 全文字段仍带标点。前端「普通识别」上传视频时按 `[HH:MM:SS] 文本` 分段展示并可复制全文。
- 上传区提示与 `accept` 已改为音频+视频（普通识别与关键词检索共用）。

## 启动提速：模型按需加载（2026-09-15）

- 三个模型全部改为懒加载：基础模型（`app.py`，首次识别/检索时）、说话人模型（首次 `/asr_spk` 时）、
  流式模型（`ws_server.py`，首个 WebSocket 连接时）。`from funasr import AutoModel` 也移到了各自函数内部
  （funasr 连带加载 torch 要十几秒，放顶层会拖慢端口就绪）。
- 效果：**双服务就绪从约 45 秒降到 2~3 秒**，浏览器立刻打开；模型在后台加载。
- 新增 `/health`（返回 `loaded_models` / `loading` / `base_ready`）与 `/warmup`（后台线程预热，不阻塞事件循环）；
  前端状态条实时显示"模型已就绪 / 正在加载"，页面打开即自动调用预热。
- 流式链路改为服务端先发 `status`（提示需加载模型）→ 加载完成发 `ready` → 客户端收到 `ready` 才开音频管线，
  避免模型加载期间推流导致音频积压、字幕严重滞后。
- `_model_lock` 双检锁保证"预热线程"与"识别请求"不会重复加载同一模型。
- 修复 `launcher.py` 日志编码：`basicConfig` 未指定 encoding 导致中文 Windows 下按 cp936 写入、查看即乱码，
  现已显式 `encoding="utf-8"`（旧日志归档为 `logs/launcher_encoding_archive.log`）。

- 新增 `code/launcher.py` 一键启动器（**无窗口常驻版，由 pythonw.exe 运行**）：
  后台静默拉起两个服务，端口就绪后自动开浏览器；
  通过 Windows 作业对象保证停止脚本结束启动器时**两个服务一并退出**，不留僵尸进程；
  防重复启动（端口占用跳过 + pids.json 去重 + 二次双击只开页面）。
- 新增 `code/stop.py` + `停止VoiceMind.bat`：先按 `logs/pids.json` 结束启动器，再按端口兜底清理。
- 新增 `启动VoiceMind.bat`（UTF-8 + chcp 65001，无窗口模式）与桌面快捷方式「VoiceMind 声智助手」（图标 `FunASR.ico`）。
- 服务输出重定向到 `logs/app.log`、`logs/ws.log`，启动器自身日志在 `logs/launcher.log`。
- `app.py` / `ws_server.py` **无需改动**：两者早已用 `__file__` 推导绝对路径，
  改成 `../web/...` 相对路径反而会依赖启动时的工作目录。

## 前端 UI 重设计记录（2026-09-15）

- 全新专业深色主题（参考 Linear / Raycast / Vercel 设计语言）：
  - 配色：背景 #0A0A0B、卡片 #141416、输入框 #1C1C1F、边框 #2A2A2E、品牌蓝 #4F7CFF；
  - 布局：顶栏 56px（Logo + 服务状态）、左侧边栏 200px（SVG 图标导航）、主区最大 900px、底部状态栏 32px（服务状态 / 模型 / 请求延迟）；
  - 视觉：圆角统一 6px、边框统一 1px、无渐变、无 emoji（全部改为内嵌 SVG symbol 图标）、时间戳用等宽字体。
- 交互逻辑与接口完全不变：四个页面（普通识别 / 说话人分离 / 关键词检索 / 实时录音）、
  模型状态条与后台预热、麦克风权限提示、导出 TXT/SRT、批量检索均照旧。
- 已用真实浏览器（Chromium）逐页截图验收，并通过 UI 走查：上传识别返回
  「你是一个大肥猫。」、关键词检索命中高亮正常。

## 实时字幕合并优化记录（2026-09-15）

- 问题：流式识别每个 600ms chunk 回一条 partial（纯增量），逐条展示把「骨肉相连」拆成四行，像分词错误。
- 前端改为字幕合并引擎（仅改 `web/index.html`，后端协议未动）：
  - partial 追加进当前"草稿行"，一行持续生长（末尾蓝色闪烁光标表示未说完）；
  - 超过 1.5 秒无新消息 → 草稿行固定，下条 partial 另起一行；
  - final（整句带标点全文）→ 草稿行作废删除，另起定稿行；
  - 复制内容按"行"收集，final 会自动替换此前被固定的无标点草稿，不会重复。
- 注意：partial 是**纯增量**，合并必须纯追加；带"包含即去重"的合并会把「看看」
  这类合法重复字误删（已在浏览器内验证）。
